# Completed Tasks: 2026-08-04

已完成或取消的 task 从 [`../../tasks.md`](../../tasks.md) 移入本文件；当前实现范围以
[`../../spec.md`](../../spec.md) 为准。

| 完成 | ID | 状态 | 场景 | 任务与记录 | 依赖 | 完成条件 |
| --- | --- | --- | --- | --- | --- | --- |
| [x] | ABD-064 | completed | US-01 | 使用 `acp-go-sdk` 实现 ACP v1 stdio provider，共用既有进程、MCP、permission、输出与关闭逻辑。 | ABD-059 | fake ACP v1 Agent contract test 通过；MCP、chunk、prompt response、permission、取消、无有效响应和非 v1 协商均验证。 |
| [x] | ABD-065 | completed | US-01 | 增加 `codex`、`hermes`、`openclaw` provider allowlist 与 profile/setup 选择。 | ABD-064 | profile 不接受任意命令；三个 ID 解析为固定入口；旧 profile 默认迁移为 `codex`。 |
| [x] | ABD-066 | completed | US-01 | 将 provider output sink 接到 event-bound Stream reply，支持 first/append/end、无 chunk 回退和取消收尾。 | ABD-064 | 同一 event 只创建一条 Stream；连续索引与结束测试通过；非单调替换显式失败且不补发第二条消息。 |
| [x] | ABD-067 | completed | US-01 | 在 `abd-im-sdk-core` 补齐通用 Stream message 创建、Append/Get、End 与通知处理。 | ABD-066 | SDK unit/fake HTTP 测试通过；SDK 不导入 ACP；CLI 可通过 daemon-owned UserContext 发送流。 |
