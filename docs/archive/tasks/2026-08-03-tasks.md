# Completed Tasks: 2026-08-03

已完成或取消的 task 从 [`../../tasks.md`](../../tasks.md) 移入本文件；当前实现范围以 [`../../spec.md`](../../spec.md) 为准。

| 完成 | ID | 状态 | 场景 | 任务与记录 | 依赖 | 完成条件 |
| --- | --- | --- | --- | --- | --- | --- |
| [x] | ABD-060 | completed | US-01, US-02, US-03 | 将首次配置、owner 授权和当前用户 daemon 生命周期收敛为一个产品级入口。 | ABD-059 | `abdim setup` 交互登录固定 ABD 部署、生成一次性 owner 配对并自动后台启动；未配对或非 owner 消息不创建 run，owner 可使用全部已验证能力；公开生命周期仅为 `start`、`stop`、`restart`、`status`；密码、token 和配对码不泄露到持久化或日志；自动化测试通过。 |
| [x] | ABD-061 | completed | US-01, US-02 | 删除未要求的第二 IM 身份和配对门槛，使 setup 后立即可对话。 | ABD-060 | setup 不再生成或保存配对状态；旧 profile 可直接启动；所有非 bot 自己发送的受支持消息可创建具有全部已验证方法的 run；自动化、发布构建和当前 profile 重启验证通过。 |
| [x] | ABD-062 | completed | US-01, US-02, US-03 | 交付默认 reply-only 和显式入站全工具开关，并使发布证据和文档与实际授权边界一致。 | ABD-061 | policy 使用真实 sender/session 上下文；self/group 事件不创建 run；默认 tool 集合为空，profile 显式启用后发现全部 `available` 方法且仍经 grant/target/window 校验；关闭等待 reply 收尾；受控 workflow 覆盖全部 `available` 方法并在缺少 fixture 时失败；自动化和发布构建通过。 |
