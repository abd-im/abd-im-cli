# Completed Tasks: 2026-08-01

已完成或取消的 task 从 [`../../tasks.md`](../../tasks.md) 移入本文件；当前实现范围以 [`../../spec.md`](../../spec.md) 为准。

| 完成 | ID | 状态 | 场景 | 任务与路径 | 依赖 | 完成条件 |
| --- | --- | --- | --- | --- | --- | --- |
| [x] | ABD-041 | completed | US-02 | 在 `internal/agent/grant/`、`internal/control/`、`internal/agent/proxy/` 和 `internal/daemon/` 将 grant target allowlist 改为按 typed method 隔离，并为异构资源定义稳定目标命名。 | ABD-009, ABD-036 | 一个 run 的读取和写入不能共享未分类 target；每个 method 只接受其显式 target；P1 读取和 `group.create` 的 target 拒绝语义及 e2e 保持通过。 |
| [x] | ABD-042 | completed | US-02 | 在 `internal/capability/message/`、`internal/bridge/abdim/`、`internal/connector/`、`internal/mcp/provider/`、`cmd/abdim/` 和 `tests/e2e/` 实现 run-scoped `message.send_text`。 | ABD-041 | 显式 grant 只能向 allowlisted 单聊用户或群发送有上限的文本；相同 key 返回原 operation，未知结果不补发；固定 SDK/server integration gate 通过；默认入站 policy 不授予该方法。 |
| [x] | ABD-043 | completed | US-02 | 在 `internal/capability/message/`、`internal/bridge/abdim/`、`internal/connector/`、`internal/mcp/provider/`、`cmd/abdim/` 和 `tests/e2e/` 实现 run-scoped `message.send_quote`。 | ABD-042 | 显式 grant 同时约束发送对象、会话和被引用消息；原消息只能从 grant 窗口内的固定 server-read source 获取；相同 key 返回原 operation，未知结果不补发；固定 SDK/server integration gate 通过。 |
| [x] | ABD-044 | completed | US-02 | 在 `internal/capability/message/`、`internal/bridge/abdim/`、`internal/connector/`、`internal/mcp/provider/`、`cmd/abdim/` 和 `tests/e2e/` 实现 run-scoped `message.send_at`。 | ABD-042 | 显式 grant 同时约束目标群和每个被 @ 用户；文本和 mention 数量有上限；相同 key 与未知结果语义保持 fail closed；固定 SDK/server integration gate 通过。 |
| [x] | ABD-045 | completed | US-02 | 在 `internal/capability/conversation/`、`internal/connector/`、`internal/mcp/provider/`、`cmd/abdim/` 和 `tests/e2e/` 实现 run-scoped `conversation.mark_read`。 | ABD-041 | 显式 grant 只能标记 allowlisted conversation 内、有限消息窗口所界定的已读边界；provider 不能传 server sequence；相同 key 返回原 operation，未知结果不补发；固定 SDK/server integration gate 通过。 |
| [x] | ABD-046 | completed | US-02 | 建立受 grant 限制的消息附件 metadata、quota 与不透明引用基础设施。详见 [`../issues/2026-08-01-ABD-046-attachment-foundation.md`](../issues/2026-08-01-ABD-046-attachment-foundation.md)。 | ABD-041 | provider 不能传递任意本地路径；每个附件引用绑定 profile、run、额度和有效期；正文与文件内容不进入 control DB、审计或日志。 |
