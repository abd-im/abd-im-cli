# Completed Tasks: 2026-08-01

已完成或取消的 task 从 [`../../tasks.md`](../../tasks.md) 移入本文件；当前实现范围以 [`../../spec.md`](../../spec.md) 为准。

| 完成 | ID | 状态 | 场景 | 任务与路径 | 依赖 | 完成条件 |
| --- | --- | --- | --- | --- | --- | --- |
| [x] | ABD-041 | completed | US-02 | 在 `internal/agent/grant/`、`internal/control/`、`internal/agent/proxy/` 和 `internal/daemon/` 将 grant target allowlist 改为按 typed method 隔离，并为异构资源定义稳定目标命名。 | ABD-009, ABD-036 | 一个 run 的读取和写入不能共享未分类 target；每个 method 只接受其显式 target；P1 读取和 `group.create` 的 target 拒绝语义及 e2e 保持通过。 |
| [x] | ABD-042 | completed | US-02 | 在 `internal/capability/message/`、`internal/bridge/abdim/`、`internal/connector/`、`internal/mcp/provider/`、`cmd/abdim/` 和 `tests/e2e/` 实现 run-scoped `message.send_text`。 | ABD-041 | 显式 grant 只能向 allowlisted 单聊用户或群发送有上限的文本；相同 key 返回原 operation，未知结果不补发；固定 SDK/server integration gate 通过；默认入站 policy 不授予该方法。 |
| [x] | ABD-043 | completed | US-02 | 在 `internal/capability/message/`、`internal/bridge/abdim/`、`internal/connector/`、`internal/mcp/provider/`、`cmd/abdim/` 和 `tests/e2e/` 实现 run-scoped `message.send_quote`。 | ABD-042 | 显式 grant 同时约束发送对象、会话和被引用消息；原消息只能从 grant 窗口内的固定 server-read source 获取；相同 key 返回原 operation，未知结果不补发；固定 SDK/server integration gate 通过。 |
| [x] | ABD-044 | completed | US-02 | 在 `internal/capability/message/`、`internal/bridge/abdim/`、`internal/connector/`、`internal/mcp/provider/`、`cmd/abdim/` 和 `tests/e2e/` 实现 run-scoped `message.send_at`。 | ABD-042 | 显式 grant 同时约束目标群和每个被 @ 用户；文本和 mention 数量有上限；相同 key 与未知结果语义保持 fail closed；固定 SDK/server integration gate 通过。 |
| [x] | ABD-045 | completed | US-02 | 在 `internal/capability/conversation/`、`internal/connector/`、`internal/mcp/provider/`、`cmd/abdim/` 和 `tests/e2e/` 实现 run-scoped `conversation.mark_read`。 | ABD-041 | 显式 grant 只能标记 allowlisted conversation 内、有限消息窗口所界定的已读边界；provider 不能传 server sequence；相同 key 返回原 operation，未知结果不补发；固定 SDK/server integration gate 通过。 |
| [x] | ABD-046 | completed | US-02 | 建立受 grant 限制的消息附件 metadata、quota 与不透明引用基础设施。详见 [`../issues/2026-08-01-ABD-046-attachment-foundation.md`](../issues/2026-08-01-ABD-046-attachment-foundation.md)。 | ABD-041 | provider 不能传递任意本地路径；每个附件引用绑定 profile、run、额度和有效期；正文与文件内容不进入 control DB、审计或日志。 |
| [x] | ABD-047 | completed | US-02 | 交付消息控制能力族：`message.send_location`、`message.send_custom` 和 `message.revoke`。详见 [`../issues/2026-08-01-ABD-047-message-controls.md`](../issues/2026-08-01-ABD-047-message-controls.md)。 | ABD-041, ABD-042 | 三个方法有独立 schema、显式 grant target、operation/idempotency 和固定 SDK/server integration gate；未知结果不补发。 |
| [x] | ABD-049 | completed | US-02 | 交付会话设置能力族：`conversation.set_pinned` 和 `conversation.set_receive_option`。详见 [`../issues/2026-08-01-ABD-049-conversation-settings.md`](../issues/2026-08-01-ABD-049-conversation-settings.md)。 | ABD-041 | 两个方法只允许单一 conversation target、固定选项和 server action；不触及 SDK local DB，未知结果 fail closed。 |
| [x] | ABD-050 | completed | US-02 | 交付好友关系能力族：`friend.request`、`friend.respond`、`friend.delete` 和 `friend.set_remark`。详见 [`../issues/2026-08-01-ABD-050-friend-relationships.md`](../issues/2026-08-01-ABD-050-friend-relationships.md)。 | ABD-041 | 四个动作绑定 profile owner 和 user target，状态验证、输入限制、operation/idempotency、unknown fail closed 和 integration gate 均通过。 |
| [x] | ABD-051 | completed | US-02 | 交付黑名单能力族：`blacklist.add` 和 `blacklist.remove`。详见 [`../issues/2026-08-01-ABD-051-blacklist-management.md`](../issues/2026-08-01-ABD-051-blacklist-management.md)。 | ABD-041 | 两个动作绑定 profile owner 和 user target，移除前验证关系，operation/idempotency、unknown fail closed 和 server source tests 通过。 |
| [x] | ABD-048 | completed | US-02 | 交付媒体与文件消息能力族：`message.send_image`、`message.send_file`、`message.send_sound` 和 `message.send_video`。详见 [`../issues/2026-08-01-ABD-048-media-messages.md`](../issues/2026-08-01-ABD-048-media-messages.md)。 | ABD-046 | 方法只接受同 profile、同 run、未过期且类型匹配的 attachment reference；文件名、大小、时长和目标均受 grant 限制；不向 provider、日志或 control DB 暴露路径或内容；未知上传或发送结果不补发，且 integration gate 通过。 |
| [x] | ABD-052 | completed | US-02 | 交付群成员关系能力族：`group.join`、`group.leave`、`group.invite_members` 和 `group.remove_members`。详见 [`../issues/2026-08-01-ABD-052-group-membership.md`](../issues/2026-08-01-ABD-052-group-membership.md)。 | ABD-041 | grant 同时约束 group 和涉及的 user；申请消息与成员数量有受限输入；离开、邀请和移除均由固定 source/action 验证当前成员及操作者权限；每个方法 fail closed 并有 integration gate。 |
| [x] | ABD-053 | completed | US-02 | 交付群管理能力族：`group.set_info`、`group.set_mute`、`group.set_member_mute` 和 `group.transfer_owner`。详见 [`../issues/2026-08-01-ABD-053-group-administration.md`](../issues/2026-08-01-ABD-053-group-administration.md)。 | ABD-041, ABD-052 | grant 同时约束 group 和成员 target；只公开有限群资料字段、mute boolean 与有上限的 member mute 时长；固定 source/action 验证管理或 owner 权限；每个方法有 operation/idempotency、未知结果 fail closed 与 integration gate。 |
