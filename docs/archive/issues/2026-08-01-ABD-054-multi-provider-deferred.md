# ABD-054: Multi Provider

状态：`deferred`（2026-08-01）

## Decision

当前交付保持单 Codex provider。多 provider adapter 选择与隔离暂不实现，避免扩大首发版本的 provider 兼容面和发布门禁。

## Reactivation

只有用户明确重新启用多 provider 范围后，才恢复 active issue，并重新确认第二 adapter、兼容矩阵和隔离验收。
