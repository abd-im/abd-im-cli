# Archived Issue Records

活动 issue record 完成后从 [`../../issues/`](../../issues/) 移入本目录。每份记录保留完成时的实现决策、验证结果和完成 commit；其活动索引位于 [`../../tasks.md`](../../tasks.md)，简要状态记录位于同日的 [`../tasks/`](../tasks/) 文件。

目前没有已归档的 issue record。
