# Tasks

只保留未完成事项：

| 状态 | 任务 | 完成条件 |
| --- | --- | --- |
| ready | 发布包含 Stream API 的 `abd-im-sdk-core` 版本 | CLI 删除本地 `replace`，独立 checkout 可下载依赖并通过测试。 |
| future | 网页 Agent 工作区 | 可按 conversation 列出历史 run、创建会话、发送输入、取消 run 并展示状态。 |
| future | 其他 Agent ACP 接入 | 每个固定 Agent 分别通过 ACP v1 的启动、CLI 调用、输出和取消测试。 |

已完成或取消的历史事项位于 `docs/archive/`。
